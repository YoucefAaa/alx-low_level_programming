#include "main.h"
#include <string.h>
/**
  * *_strcpy - a func
  * @dest : a param
  * @src : a param
  * Return: the dest
  */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
