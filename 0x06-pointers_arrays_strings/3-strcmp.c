#include "main.h"
#include <string.h>
/**
  * _strcmp - a func
  * @s1 : a param
  * @s2 : a second param
  * Return: the st var
  *
  */

int _strcmp(char *s1, char *s2)
{
	int st = strcmp(s1, s2);

	return (st);
}
