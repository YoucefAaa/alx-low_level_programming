#include "main.h"
#include <string.h>
/**
  * _strcat - a func
  * @dest : a param
  * @src : a 2nd param
  * Return: always a 0
  *
  */


char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}
