#include "main.h"
#include <stdlib.h>
/**
 * _abs - a func
 *
 * @n : the abs
 * Description: asdf
 * Return: asf
 */

int _abs(int n)
{

	if (n >= 0)
		return (n);
	else
	{
		int y = abs(n);
		_putchar(y);
	}
}
