#include "main.h"
/**
  * mul - a func to return the mulitipation of to numbers
  * @a : num 1
  * @b : num 2
  *
  * Return: the result of multi of two numbers
  */

int mul(int a, int b)
{
	return (a * b);
}
